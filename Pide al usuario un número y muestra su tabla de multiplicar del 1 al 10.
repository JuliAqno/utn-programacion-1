#include <stdio.h>

int main() {
	int n,resultado;
	printf("ingrese un numero tal que muestre la tabla de multiplicar: ");
	scanf("%d",&n);
	resultado=1;
	for ( int i=1; i<=10; i++){
	
		resultado=n*i;
		printf(" \nel resultado de %d x %d = %d",n,i,resultado);
		
		
	};
	
	
	
	
	return 0;
}

